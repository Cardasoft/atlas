[build]
target = "index.html"

[serve]
addresses = ["0.0.0.0"]
port = 8081

# Dev : `trunk serve` (front, :8081) + `cargo run -p atlas-core` (API, :8080).
# Le front appelle /v1/* ; trunk relaie vers l'API pour rester en même origine.
[[proxy]]
backend = "http://localhost:8080/v1"
